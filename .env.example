# Telegram delivery for form submissions.
# 1. Create a bot with @BotFather -> it gives you the token.
# 2. Add the bot to the chat/group that should receive leads, send it a message,
#    then read the chat id from https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
